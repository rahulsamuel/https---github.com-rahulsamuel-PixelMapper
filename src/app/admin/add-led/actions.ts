// Plain re-export — no 'use server' here; the action is defined (and registered) in add-led/actions.ts
export { addProductAction } from '@/app/add-led/actions';
export type { FormState } from '@/app/add-led/actions';
